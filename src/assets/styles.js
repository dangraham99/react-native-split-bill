import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    safearea: {
        flex: 1,
    },

    container: {
        flex: 1,
        padding: 24
    }
})

export default styles